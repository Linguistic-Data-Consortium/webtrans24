-content_for :head do
  = vite_javascript_tag 'users'

#permissions{ 'data-admin' => admin?.to_s, 'data-portal_manager' => portal_manager?.to_s, 'data-project_manager' => project_manager?.to_s }

#action_specific_js.users_init_show
